<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MadukaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maduka">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Maduka">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.shell.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.properties.AddShopActivity"
            android:exported="false" />

        <activity
            android:name=".ui.superadmin.users.RegisterAdminActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.tenants.RegisterTenantActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.payments.RecordPaymentActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.notifications.NotificationsActivity"
            android:exported="false" />

        <receiver
            android:name=".notifications.OverdueCheckReceiver"
            android:exported="false" />
    </application>
</manifest>
